import 'package:flutter_test/flutter_test.dart';

/// Kept as a smoke entrypoint. Detailed coverage lives in:
/// - `test/unit/`
/// - `test/widget/`
/// - `test/integration/`
/// - `integration_test/` (device smoke)
void main() {
  test('test suite is organized across unit, widget, and integration folders',
      () {
    expect(true, isTrue);
  });
}
